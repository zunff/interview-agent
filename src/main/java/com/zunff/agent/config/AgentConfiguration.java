package com.zunff.agent.config;

import org.springframework.context.annotation.Configuration;

/**
 * Agent 配置类
 *
 * 注意：面试 Agent 的主要配置已移至 InterviewAgentConfiguration
 * 此类保留用于未来扩展其他 Agent
 */
@Configuration
public class AgentConfiguration {
    // 面试 Agent 配置已移至 InterviewAgentConfiguration
    // 此类可用于添加其他 Agent 配置
}
